# Upstash Redis (production). Add the integration from the Vercel
# Marketplace and these are injected automatically; copy them here only
# if you want to point local development at the same store.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
